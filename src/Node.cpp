#include "EightWinds/NodeGraph/Node.h"

namespace EWE{
    void Node::Render(CommandBuffer cmdBuf) const{
        //draw the background, draw the foregound, then draw the title
        
        //i could make each vertex either a position only, or I coudl make each one a position and color, so color can be controlled per vertex
        

    }

}